import React from "react";
import styled, { keyframes } from "styled-components";

import AnimationContainer from "../helpers/AnimationContainer";

const New = (props) => {
  return <AnimationContainer {...props}></AnimationContainer>;
};

export default New;
